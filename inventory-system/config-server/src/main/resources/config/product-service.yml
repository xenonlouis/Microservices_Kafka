server:
  port: 8081

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://postgres-products:5432/products
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true 